#include <stdio.h>

// Breaks array into half until there are many arrays with one element
int divide();

// Merges those broken arrays to build a sorted array
int merge();

int main(){
    int array[] = {11,7,58,0,-9,67,54,32};
    
}